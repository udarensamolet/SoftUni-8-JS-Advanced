function ticTacToe(input) {
    let matrix = [[[false, false, false],
    [false, false, false],
    [false, false, false]]
    ];

    for (let i = 0; i < input.length; i++) {
        let position = input[i].split(' ');
        let row = position[0];
        let col = position[1];

       let playerMark = 'X';

        
       
    }
    function playerX(row, col) {
    }
}